using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.InputSystem;
using UnityEngine.InputSystem.Controls;

public class SceneManager : MonoBehaviour
{
public void goToGame()
    {
        UnityEngine.SceneManagement.SceneManager.LoadScene("player select");
    }

public void Update()
    {

     if (Keyboard.current.rightArrowKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.leftArrowKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.upArrowKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.downArrowKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.qKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.wKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.eKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.rKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.tKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.yKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.uKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.iKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.oKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.pKey.wasPressedThisFrame)
        goToGame();

     if (Keyboard.current.aKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.sKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.dKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.fKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.gKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.hKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.jKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.kKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.lKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.zKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.xKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.cKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.vKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.bKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.nKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.mKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.tabKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.shiftKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.ctrlKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.altKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.leftMetaKey.wasPressedThisFrame)
        goToGame();

     if (Keyboard.current.rightMetaKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.leftCommandKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.rightCommandKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.leftWindowsKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.rightWindowsKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.spaceKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.enterKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.backspaceKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.quoteKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.semicolonKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.numpadMinusKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.numpadPlusKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.periodKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.commaKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.slashKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.backslashKey.wasPressedThisFrame)
        goToGame();

         if (Keyboard.current.capsLockKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.altKey.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f1Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f2Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f3Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f4Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f5Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f6Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f7Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f8Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f9Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f10Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f11Key.wasPressedThisFrame)
        goToGame();

        if (Keyboard.current.f12Key.wasPressedThisFrame)
        goToGame();
}   
}